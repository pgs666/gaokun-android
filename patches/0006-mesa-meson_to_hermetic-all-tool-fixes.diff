diff --git a/meson_to_hermetic/meson_common.py b/meson_to_hermetic/meson_common.py
index bac4a3ec4e0..024ab075566 100644
--- a/meson_to_hermetic/meson_common.py
+++ b/meson_to_hermetic/meson_common.py
@@ -67,7 +67,10 @@ def declare_dependency(
     )
 
 
-def find_program(name: str, required=False, native=False, disabler=False, version=''):
+def find_program(name: str, *alt_names, required=False, native=False, disabler=False, version=''):
+    # gaokun 修复：meson 支持多候选名 find_program('flex', 'lex', ...)；
+    # 多余的位置参数在这里被收进 alt_names，不再污染 required。
+    del alt_names
     if type(required) is impl.FeatureOption:
         required = required.state == impl.EnableState.ENABLED
     if type(required) is not bool:
@@ -89,10 +92,14 @@ def find_program(name: str, required=False, native=False, disabler=False, versio
         or name == 'install_megadrivers.py'
         or name == 'nm'
         or name == 'python'
+        or name == 'python3'  # gaokun: mesa 25.3 改用 python3
         or name == 'symbols-check.py'
         or name == 'sphinx-build'
     ):
-        return impl.Program(name, found=required)
+        # gaokun: PATH 里真有就算找到（原来只看 required，导致
+        # prog_python.found() 为假，custom_target 断言失败）
+        import shutil
+        return impl.Program(name, found=(shutil.which(name) is not None) or required)
 
     exit('Unhandled program check: ' + name)
 
@@ -222,7 +229,7 @@ def get_option(name):
     if name == 'debug':
         return True
     if name == 'b_sanitize':
-        return False
+        return 'none'  # gaokun: 生成代码按字符串用
     if name == 'backend':
         return 'custom'
 
@@ -234,7 +241,8 @@ def project(name, language_list, version, license, meson_version, default_option
 
 
 def run_command(program, *commands, check=False):
-    return program.run_command(commands)
+    # gaokun 修复：解包 commands，并开启输出捕获（调用方会 .stdout()）
+    return program.run_command(*commands, capture_output=True)
 
 
 def environment():
diff --git a/meson_to_hermetic/meson_impl.py b/meson_to_hermetic/meson_impl.py
index 6f0be258df8..6e8a8af5352 100644
--- a/meson_to_hermetic/meson_impl.py
+++ b/meson_to_hermetic/meson_impl.py
@@ -61,6 +61,13 @@ class Machine:
     def set_system(self, system: str):
         self._system = system
 
+    def endian(self, *a, **k):
+
+        # gaokun: aarch64/x86 都是小端
+
+        return 'little'
+
+
     def cpu_family(self):
         return self._cpu_family
 
@@ -128,6 +135,54 @@ class Dependency:
         return self.unique_id == other.unique_id
 
 
+def _gaokun_ver_tuple(text):
+    parts = []
+    for chunk in str(text).strip().split('.'):
+        digits = ''
+        for ch in chunk:
+            if ch.isdigit():
+                digits += ch
+            else:
+                break
+        parts.append(int(digits) if digits else 0)
+    return tuple(parts)
+
+
+class MesonStr(str):
+    """带 meson 语言方法的字符串（gaokun 补丁）。"""
+
+    def version_compare(self, spec):
+        spec = str(spec).strip()
+        for op in ('>=', '<=', '==', '!=', '>', '<'):
+            if spec.startswith(op):
+                rhs = spec[len(op):].strip()
+                break
+        else:
+            op, rhs = '==', spec
+        a = _gaokun_ver_tuple(self)
+        b = _gaokun_ver_tuple(rhs)
+        width = max(len(a), len(b))
+        a = a + (0,) * (width - len(a))
+        b = b + (0,) * (width - len(b))
+        if op == '>=':
+            return a >= b
+        if op == '<=':
+            return a <= b
+        if op == '==':
+            return a == b
+        if op == '!=':
+            return a != b
+        if op == '>':
+            return a > b
+        return a < b
+
+    def split(self, *args, **kwargs):
+        return [MesonStr(s) for s in str.split(self, *args, **kwargs)]
+
+    def strip(self, *args, **kwargs):
+        return MesonStr(str.strip(self, *args, **kwargs))
+
+
 class CommandReturn:
     def __init__(self, completed_process):
         self.completed_process = completed_process
@@ -136,7 +191,11 @@ class CommandReturn:
         return self.completed_process.returncode
 
     def stdout(self):
-        return self.completed_process.stdout
+        # gaokun: capture_output 下是 bytes；返回带 meson 语义的字符串
+        out = self.completed_process.stdout
+        if isinstance(out, bytes):
+            out = out.decode("utf-8", "replace")
+        return MesonStr(out if out is not None else "")
 
 
 class Program:
@@ -195,6 +254,21 @@ class FeatureOption:
             self.state = EnableState.DISABLED
         return self
 
+    def enable_if(self, value: bool, error_message: str = ''):
+
+        # gaokun 补丁：meson 0.59+ API，工具缺失
+
+        if not value:
+
+            return self
+
+        if self.state == EnableState.DISABLED:
+
+            exit(error_message)
+
+        return FeatureOption(self.name, state=EnableState.ENABLED)
+
+
     def disable_if(self, value: bool, error_message: str):
         if not value:
             return self
@@ -274,6 +348,10 @@ class Environment:
 
 
 class StaticLibrary:
+    def __iter__(self):
+        # gaokun: meson 列表 += 单对象 是追加语义
+        return iter([self])
+
     def __init__(self, target_name, link_with=[], link_whole=[]):
         self.target_name = target_name
         self.link_with = link_with
@@ -288,17 +366,29 @@ class SharedLibrary:
 
 
 class Executable:
+    def __iter__(self):
+        # gaokun: meson 列表 += 单对象 是追加语义
+        return iter([self])
+
     def __init__(self, name):
         self.name = name
 
 
 class CustomTargetItem:
+    def __iter__(self):
+        # gaokun: meson 列表 += 单对象 是追加语义
+        return iter([self])
+
     def __init__(self, custom_target, index):
         self.target = custom_target
         self.index = index
 
 
 class CustomTarget:
+    def __iter__(self):
+        # gaokun: meson 列表 += 单对象 是追加语义
+        return iter([self])
+
     def __init__(self, name, outputs=[], generates_h=False, generates_c=False):
         self._name = name
         self._outputs = outputs
@@ -358,6 +448,18 @@ class Meson:
     def project_version(self):
         return _gProjectVersion
 
+    def global_source_root(self, *args, **kwargs):
+
+        # gaokun: 非子项目场景与 project_source_root 等价
+
+        return self.project_source_root(*args, **kwargs)
+
+
+    def global_build_root(self, *args, **kwargs):
+
+        return self.project_build_root(*args, **kwargs)
+
+
     def project_source_root(self):
         return os.getcwd()
 
@@ -424,6 +526,9 @@ class Compiler(ABC):
             function == 'qsort_s'
             or function == 'pthread_setaffinity_np'
             or function == 'secure_getenv'
+            # gaokun: clang 没有 GCC 的 *_overflow_p 变体
+            or function == '__builtin_add_overflow_p'
+            or function == '__builtin_sub_overflow_p'
         ):
             return False
         return True
@@ -658,6 +763,18 @@ def dependency(*names, required=True, version=''):
         if name == '':
             return Dependency('null', version, found=False)
 
+        # gaokun: libdrm 在 AOSP 里是 vendor_available 共享库，报告为已找到；
+        # 否则 mesa 跳过 vk_drm_syncobj.c，turnip 链接期缺符号。
+        if name == 'libdrm':
+            return Dependency(
+                name,
+                targets=[DependencyTarget('libdrm', DependencyTargetType(1))],
+                version=version,
+                found=True,
+            )
+        # gaokun: 关掉 gralloc 元数据后端（IMapper4/5）——
+        # 需要 libui 的 Singleton 静态成员，vendor 变体不导出；
+        # 本机 gralloc 是 minigbm，走 u_gralloc_cros_api 即可。
         if name in external_dep:
             targets = external_dep.get(name)
             return Dependency(
@@ -678,7 +795,6 @@ def dependency(*names, required=True, version=''):
             or name == 'libmagma_virt'
             or name == 'libva'
             or name == 'libzstd'
-            or name == 'libdrm'
             or name == 'libglvnd'
             or name == 'libudev'
             or name == 'libunwind'
diff --git a/meson_to_hermetic/meson_to_hermetic.py b/meson_to_hermetic/meson_to_hermetic.py
index 2930bcd80e8..d3b5a6957a8 100644
--- a/meson_to_hermetic/meson_to_hermetic.py
+++ b/meson_to_hermetic/meson_to_hermetic.py
@@ -399,9 +399,35 @@ def include_directories(*paths, is_system=False):
     return _gIncludeDirectories[name]
 
 
+class GaokunFsModule:
+    """Minimal 'fs' module stub (gaokun): Android build never compiles OpenCL,
+    so the -fmacro-prefix-map prefix from fs.relative_to() is irrelevant."""
+
+    def relative_to(self, a, b, *args, **kwargs):
+        return "."
+
+    def exists(self, path, *args, **kwargs):
+        import os
+        return os.path.exists(str(path))
+
+    def is_dir(self, path, *args, **kwargs):
+        import os
+        return os.path.isdir(str(path))
+
+    def name(self, path, *args, **kwargs):
+        import os
+        return os.path.basename(str(path))
+
+    def parent(self, path, *args, **kwargs):
+        import os
+        return os.path.dirname(str(path))
+
+
 def module_import(name: str):
     if name == 'python':
         return impl.PythonModule()
+    if name == 'fs':
+        return GaokunFsModule()
     if name == 'pkgconfig' and meson_translator.host_machine.lower() == 'fuchsia':
         return BazelPkgConfigModule()
     if name == 'pkgconfig' and meson_translator.host_machine.lower() == 'android':
@@ -429,6 +455,8 @@ def dependency(
     fallback=[],
     include_type='',
     native=False,
+    default_options=[],  # gaokun: mesa 26 传这个参数
+    disabler=False,
 ):
     return impl.dependency(*names, required=required, version=version)
 
@@ -967,6 +995,12 @@ def _get_command_args(
                     args.append(item)
             continue
 
+        if isinstance(command_item, impl.Program):
+            # gaokun: mesa 26 会把 prog_glslang 放在命令中段（经 python 包装）；
+            # 输出裸程序名，后续合并脚本会改写成 $(location) 并补 tools。
+            args.append(command_item.command if isinstance(command_item.command, str) else command_item.command[0])
+            continue
+
         assert type(command_item) is str
         match = re.match(r'@INPUT([0-9])?@', command_item)
         if match is not None:
@@ -1098,7 +1132,8 @@ def _process_wrapped_args_for_python(
 
 # TODO(bpnguyen): merge custom_target
 def custom_target(
-    target_name: str,
+    target_name: str = None,  # gaokun: meson 允许匿名，见下方推导
+
     build_always=False,
     build_always_stale=False,
     build_by_default=False,
@@ -1117,6 +1152,13 @@ def custom_target(
     install_tag=[],
     output=[],
 ):
+    # gaokun: 匿名 custom_target —— 用 output（或首个 input）推导名字
+    if target_name is None:
+        _o = output if not isinstance(output, list) else (output[0] if output else None)
+        if _o is None:
+            _i = input if not isinstance(input, list) else (input[0] if input else 'anon')
+            _o = str(_i)
+        target_name = str(_o).replace('/', '_')
     target_name = _process_target_name(target_name)
     print('Custom target: ' + target_name)
     assert type(command) is list
diff --git a/meson_to_hermetic/templates/generate_python_build.txt b/meson_to_hermetic/templates/generate_python_build.txt
index d72028ef7e2..031fbcc2e58 100644
--- a/meson_to_hermetic/templates/generate_python_build.txt
+++ b/meson_to_hermetic/templates/generate_python_build.txt
@@ -41,6 +41,8 @@ def get_variable(name: str):
 meson = impl.Meson(meson_translator.generator)
 host_machine = impl.Machine(meson_translator.host_machine, meson_translator.config.cpu, meson_translator.config.cpu_family)
 build_machine = impl.Machine(meson_translator.build_machine, meson_translator.config.cpu, meson_translator.config.cpu_family)
+# gaokun: mesa 26 引用 target_machine（交叉编译语义 = host_machine）
+target_machine = impl.Machine(meson_translator.host_machine, meson_translator.config.cpu, meson_translator.config.cpu_family)
 
 ### Load dependencies
 load_dependencies()
