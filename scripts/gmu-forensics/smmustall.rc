service smmustall /vendor/bin/smmu-nostall.sh
    user root
    group root
    seclabel u:r:shell:s0
    disabled

# 注意：**不加 oneshot** —— 脚本是无限循环的守护进程，万一被 OOM/异常打死，
# init 要能自动把它拉起来（稳定性直接建立在它身上）。
on post-fs-data
    start smmustall
