// hexagonrpc 的 Android.bp（上游自带）在 hexagonrpc_defaults 里
// header_libs 了 "device_kernel_headers" —— 那是个【约定由设备树提供】的名字，
// 树里没有任何设备定义它，所以必须我们建。
//
// ★ 但它不需要真的提供什么：hexagonrpc 用到的 6 个 fastrpc ioctl
//   （含关键的 FASTRPC_IOCTL_INIT_ATTACH_SNS）和 5 个 uapi struct，
//   bionic 的 libc/kernel/uapi/misc/fastrpc.h 里全都有（2026-08-20 逐符号比对）。
//   所以这里是个空壳，只为满足 header_libs 的名字解析。
//
// 将来若 hexagonrpc 需要比 bionic 更新的 uapi 头，就把内核的
// include/uapi 拷进本目录并加 export_include_dirs。
cc_library_headers {
    name: "device_kernel_headers",
    vendor: true,
}
