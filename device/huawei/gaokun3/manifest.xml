<?xml version="1.0" encoding="utf-8"?>
<!--
  VINTF device manifest — Huawei MateBook E Go (gaokun3)

  target-level 就是 Shipping FCM Version。缺了它构建会在最后的
  check_vintf 阶段失败：
      ERROR: Device manifest does not specify Shipping FCM Version.

  202504 的依据（均来自 AOSP 16 源码，不是猜的）：
    system/libvintf/include/vintf/Level.h        B = 202504   （B = Baklava = Android 16）
    out/soong/soong.<product>.variables          "RELEASE_BOARD_API_LEVEL": "202504"

  ─────────────────────────────────────────────────────────────────────
  ⚠️ 这里曾经声明过 android.hardware.media.c2 IComponentStore/software，
     是追查"解码器一个都没有"（docs/stage4-findings.md #36）时的一次尝试。
     【那条归因是错的，声明也已删除】：

     真凶是 media.c2.hal.selection 默认为 hidl
     （frameworks/av/media/codec2/hal/common/HalSelection.cpp:57），
     而 HIDL Codec2 在 Android 15+ 已随 hwservicemanager 一起消失。
     在 PRODUCT_SYSTEM_EXT_PROPERTIES 里设成 aidl 后解码器 0 → 66。

     那条声明不但没用，还有两个实际害处：
       1. 服务被列两次（系统侧 manifest_media_c2_software.xml 已经声明了）
       2. ★ 直接卡构建：Stage 6 M3 的整树构建在 check_vintf 上失败 ——
            ERROR: files are incompatible: The following instances are in
            the device manifest but not specified in framework
            compatibility matrix:
                android.hardware.media.c2.IComponentStore/software (@1)
          因为它是【平台 HAL】，只能由 framework compatibility matrix 认领，
          设备 manifest 单方面声明就成了"多出来的实例"。
  ─────────────────────────────────────────────────────────────────────
-->
<manifest version="1.0" type="device" target-level="202504">
</manifest>
