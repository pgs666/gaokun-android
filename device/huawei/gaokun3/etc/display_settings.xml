<?xml version="1.0" encoding="utf-8"?>
<!--
  WindowManager 每显示器设置 —— Huawei MateBook E Go (gaokun3)

  ★ 为什么需要这个文件（2026-08-20 M4 实测）：
    Android 在大屏设备上**默认 `ignoreOrientationRequest=true`** ——
    应用请求横屏时，系统【不转屏】，而是把应用「信箱化」塞进竖屏里。
    实测症状：原神启动后画面被压在 1600x1000 的一条里，
    `dumpsys activity activities` 显示
        requestedOrientation=SCREEN_ORIENTATION_LANDSCAPE   （MainActivity 确实要横屏）
        areBoundsLetterboxed=true
        letterboxReason=FIXED_ORIENTATION
    而 `dumpsys window` 显示
        mHasSetIgnoreOrientationRequest=false ignoreOrientationRequest=true
    （`false` 表示没人设过 → 这就是平台默认值，不是我们配错了。）

  ⚠️ 这**不是**"没有加速度计"导致的。本机确实没有任何传感器
     （DTS 里没有节点，见 docs/stage6-crdroid.md M4 §5），但那只影响
     "自动旋转"；应用**自己请求**的固定方向本来与传感器无关，
     是被上面这个大屏策略挡掉的。两件事别混。

  运行时等效命令（立刻生效、不用重启，但**不持久**）：
      adb shell wm set-ignore-orientation-request false
  所以要靠本文件把它做成开机默认。

  ─── 格式依据（从设备上的 services.jar 里抠出来的字符串，不是凭记忆）───
    标签：display-settings / config / display
    属性：identifier / name / ignoreOrientationRequest / windowingMode /
          forcedWidth / forcedHeight / forcedDensity / userRotation /
          userRotationMode / fixedToUserRotation / shouldShowSystemDecors /
          dontMoveToTop
    框架读取的基准路径是 `<vendor>/etc/display_settings.xml`
    （services.jar 里的字符串 "etc/display_settings.xml"；可写覆盖层在
      "system/display_settings.xml" 即 /data/system/ 下）。
    不写 <config> 时 identifier 默认按 UNIQUE_ID 匹配，
    本机内置屏的 uniqueId = `local:0`（`dumpsys display` 实测）。
-->
<display-settings>
    <display name="local:0" ignoreOrientationRequest="false" />
</display-settings>
